// Import the strict LayerType union
import type { LayerType } from "./types";

/**
 * Allowed layer types for the rendering engine.
 */
export const VALID_LAYER_TYPES: LayerType[] = ["image", "video", "text", "color"];
